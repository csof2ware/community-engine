# Day 1 — Subir o stack

## Missao
Docker compose up + boot.sh; chain, redis, postgres, graph, ipfs no ar.

## Prova on-chain
boot.log mostra RPC pronto + 2 contratos deployados + worker/API.

## Criterio de aprovacao
Mod confirma /health 200 e 5 servicos saudaveis.
